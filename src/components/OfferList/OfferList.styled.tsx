import styled from "styled-components";

export const ListContainer = styled.div`
  display: flex;
  flex-direction: column;
  flex: 1;
  padding: 14px;
  gap: 14px;
  height: 60%;
  overflow-x: hidden;
  overflow-y: auto;
`;
